import React from 'react';

export default function MultipleLinearRegression() {
  return (
    <div className="p-6 max-w-3xl mx-auto">
      <h1 className="text-3xl font-bold mb-4">Multiple Linear Regression</h1>
      <p className="text-lg mb-2">
        Multiple Linear Regression models the relationship between multiple independent variables (X₁, X₂, ..., Xₙ) and a dependent variable (Y).
      </p>
      <p>Equation: <strong>Y = a₁X₁ + a₂X₂ + ... + aₙXₙ + b</strong></p>
    </div>
  );
}
